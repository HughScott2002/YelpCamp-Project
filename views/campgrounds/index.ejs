<% layout('layouts/boilerplate') %> 
<h1>ALL Campgrounds</h1>

<div>
    <% for (let campground of campgrounds){ %>
        <div class="card mb-3">
            <div class="row">
                <div class="col-md-4">
                    <img src=" <%= campground.image %> " alt="" class="img-fluid">
                </div>
                <div class="col-md-8">
                    <div class="card-boy">
                        <h5 class="card-title"><%= campground.title %> </h5>
                        <p class="card-text"><%= campground.description %> </p>
                        <p class="card-text">
                            <small class="text-muted"><%= campground.location %></small>
                        </p>
                        <a class="btn btn-primary" href="/campgrounds/<%= campground.id %>">View <%= campground.title %> </a>
                        <a class="btn btn-info" href="/campgrounds/new"> New Campground</a>
                    </div>
                </div>
            </div>
        </div>
    <%} %>
</div>


<!-- 
<ul>
    <%# for (let campground of campgrounds){ %>
        <li><a href="/campgrounds/<%#campground._id%>""> <%# campground.title %> </a>
            <a href="/campgrounds/new">New</a>
            <a href="/campgrounds/<%#campground._id%>/edit">Edit</a>
        </li>
    <%#} %> 
</ul> -->
